{
  "name": "sentiment-dashboard",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "scripts": {
    "test": "jest --coverage --config jest.config.js",
    "test:api": "cd api && npx jest --coverage",
    "test:all": "npm run test:api && npm test"
  }
}
